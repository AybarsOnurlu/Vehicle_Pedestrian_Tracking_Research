"""
Unit Tests: Annotation Format Converter.

Tests cover:
    - KITTI TXT → YOLO format conversion
    - MOTChallenge CSV → YOLO format conversion
    - COCO JSON → YOLO format conversion
    - BDD100K JSON → YOLO format conversion
    - Coordinate normalization correctness (0.0–1.0 range)
    - Class ID mapping across dataset schemas
    - Edge cases: empty annotations, missing fields, invalid bbox coordinates
    - Batch directory processing with correct file output
"""

# TODO: Implement test cases
# import pytest
# from src.preprocessing.format_converter import FormatConverter
